---
import * as prismicH from '@prismicio/helpers';
import { Debug } from 'astro/components';
import { Image } from '@astrojs/image/components';

const {name, logo, description} = Astro.props;

const renderedName = prismicH.asHTML(name);
const renderedDescription = prismicH.asHTML(description);

---
<!-- <Debug {logo.small} /> -->
<Image class="basis-1/2" src={logo.small.url} width={logo.small.dimensions.width} height={logo.small.dimensions.height}></Image>